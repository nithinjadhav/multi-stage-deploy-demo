name: Legacy promote workflow

on:
    workflow_dispatch:

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - name: Use deploy workflow
              run: echo "This workflow is deprecated. Use .github/workflows/deploy.yml instead."
